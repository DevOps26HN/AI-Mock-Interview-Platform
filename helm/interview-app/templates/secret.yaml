{{- include "interview-app.validateTUMID" . -}}
apiVersion: v1
kind: Secret
metadata:
  name: ai-mock-interview-secret
  namespace: ai-mock-interview
type: Opaque
data:
  openai-api-key: {{ required "server.openaiApiKey is required. Please set server.openaiApiKey in values.yaml or pass it via --set." .Values.server.openaiApiKey | b64enc | quote }}
